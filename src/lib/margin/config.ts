// Shared between the HPP admin page (/admin/hpp, live Product/AddonOption
// costPrice) and the Dashboard Margin section (snapshotted OrderItem
// costPrice) — one number so "margin rendah" means the same thing in both
// places.
export const LOW_MARGIN_THRESHOLD_PERCENT = 10;
